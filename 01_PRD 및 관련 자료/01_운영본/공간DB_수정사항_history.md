## 15. 2026-04-09 ??/??/??/???? ??? ??

- ???? ??? `?? ?? ?? ? ??/???`? ?? ??? ????, `?? ??? ?? ?? ??`? ? ??? ???.
- ???? ??? `??? ?? ?? ??`? ? ??? ????, ??? ?? ??/??? ??? ?? ??? ????.
- ???? ??? ??? ?? ??, ?? ?? ??, ??? ???? ?? ?? ?? ??? ?????, ???/?? ??? ??? ?? ??? ???.
- ???? ??? `?? ??? ? ??`, `??/??? ??? ??` ??? ???.
- ??? ?? ?? ??? `percentAxisAuto()`? ???? 0~100 ?? ?? ? ??? ?? Y?? ?? ????? ???.
- ?? ????? ???? ?? ?? ??????? ????, ?? ?? ????? ??? ???.
## 14. 2026-04-09 ????/?? ??? ?? ?? ??

- ???? ? ?? KPI? `???+??????_? ?? ??`, `???+??????_? ?? ??`, `OA ?? ??` 3?? ???.
- `??? ???+??????_? ?? ??`? ? ??? ???.
- `?? ???/?? ?? ??? ???+??????_? ?? ??`, `?? ??? OA ?? ??`? ??? ??? ???.
- ?? ??? ?? ??? ?? ????, ??? ?? ??? `?? ??? ?? ?? ??`, `??? ?? ?? ??`, `??? ??? ?? ??`? ? ??? ???.
# 공간DB 수정사항 History

작성일: 2026-04-08  
작성 목적: 공간DB HTML 대시보드, Tableau 기준, PRD 개정 사항을 한 문서에서 추적하기 위한 변경 이력 기록

## 1. 문서 운영 방식

본 파일은 `변경 이력 누적 문서`이다.

- 매일 작업하면서 발생한 수정, 변경, 추가 사항을 이 파일 하단에 계속 누적 기록한다.
- 작업일마다 새 history 파일을 만들지 않는다.
- 같은 날 여러 작업을 했다면 같은 날짜 아래에 작업 단위로 이어서 기록한다.
- PRD 개정이 발생하면, 해당 작업 요약과 반영 범위를 이 파일에도 함께 남긴다.

관련 운영 원칙:

- 운영본 PRD는 매 작업 반영 후 새 버전 파일로 저장한다.
- 직전 운영본 PRD는 `02_archive` 폴더로 이동한다.
- 따라서 `PRD 파일은 버전별로 분리`, `History 파일은 누적형 단일 파일 유지`가 원칙이다.
- 운영본 PRD 파일명 규칙은 `공간DB_PRD_v버전_YYYYMMDD.md`를 사용한다.

## 2. 문서 목적

본 문서는 2026-04-07 기준으로 공간DB 대시보드에서 수행한 계산식 정비, UI/UX 개편, 필터 구조 변경, 문서 개정 사항을 기록한다.  
원칙은 다음과 같다.

- Tableau 대시보드와 HTML 대시보드의 산출 기준을 최대한 동일하게 맞춘다.
- PRD에 없거나 모호했던 운영 기준은 실제 합의된 방식으로 명문화한다.
- 향후 운영자가 변경 이력을 빠르게 추적할 수 있도록 원인, 수정 내용, 최종 기준을 함께 남긴다.

## 3. 2026-04-07 1차 정비

### 2.1 계산식 및 산출 기준 정리

- HTML 대시보드와 Tableau 워크북의 지표별 계산 방식을 대조 검토
- 회의, 소셜, 지원, 개괄, 업무 지표의 분모/분자 기준 재정의
- 연도 추이형 지표의 집계 방식을 `연도 합산 후 비율`에서 `도면별 비율 평균`으로 수정
- 회의 지표에서 `기타회의공간` 제외 기준 통일
- 지원공간 평균 개소는 `해당 공간이 존재하는 도면만 평균 포함`으로 정리
- 연도 추이 시각화는 `2020~2025`만 표시
- 모든 숫자 표시는 소수점 둘째 자리까지 통일

### 2.2 필터 구조 개편

- 단일 선택 드롭다운 중심 구조를 다중 선택 체크박스 기반 필터 UI로 변경
- 필터를 다음 3개 카테고리로 재구성
- 도면 관련 필터링: `연도`, `가용면적구간`, `인원 구간`, `프라임 오피스`, `참고도면`
- 위치 관련 필터: `주소`, `권역`
- 기업 관련 필터: `산업군`, `사업장 유형`, `대기업 여부`, `소속 대기업`
- 화면 라벨 `업종`을 `산업군`으로 변경
- `참고도면`, `대기업 여부`, `소속 대기업`, `프라임오피스` 필터 추가
- `프라임오피스`는 `O`, `X`, `사옥` 3개 값을 각각 별도 옵션으로 노출
- 동일 필드 내 다중 선택은 OR, 서로 다른 필드 조합은 AND 로 동작하도록 수정

### 2.3 레이아웃 및 탐색 UX 개편

- 상단 고정 필터 바를 폐지하고 좌측 고정 사이드 필터 패널로 변경
- 필터 패널 접기/펼치기 기능 추가
- 탭 네비게이션을 floating 구조에서 헤더 내 고정 구조로 이동
- 우측 바로가기 패널 추가
- 바로가기 클릭 시 지표 제목이 헤더에 가려지지 않도록 scroll offset 적용
- 스크롤 위치에 따라 현재 보고 있는 카드가 우측 바로가기에서 자동 하이라이트되도록 수정

### 2.4 개괄 탭 표현 방식 변경

- `인원 구간별 도면 수`, `가용 면적 구간별 도면 수`를 그래프에서 자연수 기반 히트맵 표로 변경
- `인원 구간별 가용 면적 범위`, `가용 면적 구간별 인원 범위`는 박스플롯과 함께 표를 제공하는 구조로 개편
- 표에는 `중앙값`, `적정 최소값(Q1)`, `적정 최대값(Q3)`를 명시
- `오피스 공간 프로그램 구성 비율 추이`는 전체 1개 + 2025→2020 연도별 누적 가로 막대 구조로 변경
- 막대 내부에 비율 수치를 직접 표기하여 툴팁 의존도를 낮춤

## 3. 지표별 계산식 정정 이력

### 3.1 개괄

#### 3.1.1 연도별 공간 프로그램 구성 비율 추이

기존 문제:

- HTML은 연도 내 전체 면적을 합산한 뒤 프로그램 비율을 계산하고 있었음
- 이 방식은 큰 도면에 과도한 가중치를 부여하여 Tableau 수치와 불일치가 발생함

수정 내용:

- 각 도면에서 `프로그램 면적 / 도면 가용면적`을 먼저 계산
- 이후 같은 연도에 속한 도면별 비율의 평균을 산출

최종 기준:

- 연도별 비율 지표는 모두 `도면별 비율 평균` 방식 사용

### 3.2 업무공간 : 일반

#### 3.2.1 데스크 형태별 사용 비중

기존 문제:

- HTML은 전체 좌석 합산 기준 비율에 가까운 방식으로 계산되어 Tableau와 차이가 발생함

수정 내용:

- 도면별 데스크 형태 사용 인원 비중을 먼저 계산
- 이후 연도별 평균값으로 집계

최종 기준:

- `도면별 비중 산출 → 평균`

#### 3.2.2 데스크 너비별 사용 비중

기존 문제:

- 데스크 형태별 사용 비중과 동일한 오류 패턴 존재

수정 내용:

- 도면별 데스크 너비 사용 인원 비중을 먼저 계산한 뒤 평균 집계

최종 기준:

- `도면별 비중 산출 → 평균`

### 3.3 회의공간

#### 3.3.1 회의실 평균 개소

최종 기준:

- `프로그램='회의'`
- `오픈실='실'`
- `세부유형!='기타회의공간'`
- 회의실이 1개 이상 존재하는 도면만 평균 분모에 포함

#### 3.3.2 회의실 규모별 평균 개소

기존 문제:

- 규모별 지표를 계산할 때 `해당 규모가 있는 도면만` 분모에 넣어 평균이 과대 계산됨
- 예시: `150-199인` 필터 기준 `4인이하실` 평균이 HTML에서 약 5.0으로 부풀려짐

수정 내용:

- 규모별 평균 개소의 분모를 `회의실이 하나 이상 존재하는 도면 전체`로 통일
- 각 규모별 개수는 같은 공통 분모 기준으로 평균 처리

최종 기준:

- 규모별 평균 개소는 `subtype-positive drawing average`가 아니라 `meeting-bearing drawing average`

#### 3.3.3 오픈/실 회의 지표

최종 기준:

- `기타회의공간`은 전부 제외
- 오픈/실 구분 비중도 동일한 제외 기준을 적용

### 3.4 소셜공간

#### 3.4.1 소셜 공간 정의

기존 문제:

- HTML 내부에서 실제 데이터의 프로그램 값 `공용`이 아니라 화면용 개념인 `소셜`을 직접 기준으로 사용하는 부분이 있었음

수정 내용:

- 계산 로직은 모두 원천 데이터의 `공용` 프로그램 값을 사용
- 화면 표시만 `소셜공간` 또는 `소셜`로 표현

#### 3.4.2 소셜 공간 내 유형별 구성 비율

기존 문제:

- 소셜 내부 비중이 아니라 도면 전체 면적 대비 비중으로 계산되던 문제 존재

수정 내용:

- 각 도면에서 `세부유형 면적 / 공용 총면적`을 계산
- 이후 연도별로 도면 평균 비율을 산출

최종 기준:

- 분모는 반드시 `공용 총면적`
- 연도 집계는 `도면별 비율 평균`

### 3.5 지원공간

#### 3.5.1 폰부스 및 선택업무공간_실 평균 개소

최종 기준:

- 해당 공간이 존재하는 도면만 평균 분모에 포함

#### 3.5.2 OA 평균 개소

최종 기준:

- OA가 존재하는 도면만 평균 분모에 포함

비고:

- 이 기준은 사용자 의사결정에 따라 의도적으로 유지됨

### 3.6 업무공간 : 임원

#### 3.6.1 가구구성별 평균 면적

기존 문제:

- 실형/오픈형과 세부유형 매핑 오류로 일부 값이 사실상 0에 가깝게 계산됨

수정 내용:

- 임원 세부유형 매핑 테이블을 정비하여 실형/오픈형별 평균 면적이 실제 데이터와 일치하도록 수정

## 4. 2026-04-07 2차 정비: 업무공간 : 일반 탭 개편

### 4.1 상단 KPI 구조 개편

기존 구성:

- 평균 인당면적
- 자율좌석제 비중
- 그룹 업무 공간 보유
- 주요 데스크 형태

변경 후 구성:

- 오피스 공간 프로그램 중 `업무 공간` 비중
- 평균 인당 면적
- 주요 등간격(일자형)
- 주요 등간격(L자형)
- 주요 데스크 형태
- 주요 데스크 너비

### 4.2 인당 면적 카드 개편

- 전체 평균 인당 면적을 KPI로 노출
- 카드 내부에서는 연도 구분 없는 전체 값과 연도별 추세를 함께 제공
- 그래프 라벨이 x축과 겹치지 않도록 y축 범위와 데이터 라벨 위치 조정
- 커서를 올리지 않아도 값이 바로 보이도록 표기 방식 강화

### 4.3 자율좌석제 시행 비중 개편

- 전체 도면 기준 값은 그래프가 아니라 큰 수치 카드로 분리
- 연도별 추세는 별도 그래프로 제공
- 전체 값과 연도별 값이 한눈에 구분되도록 레이아웃 분리

### 4.4 데스크 형태별/너비별 사용 비중 개편

- `전체 도면 기준`과 `연도별 평균 추세`를 같은 지표 카드에서 함께 볼 수 있도록 구조 개편
- 차트 안에서 전체와 연도별 구간이 시각적으로 분리되도록 보조선 및 구획 추가
- 막대/라벨이 카드 밖으로 밀리지 않도록 데이터 라벨 위치 조정
- 수치를 툴팁 없이 읽을 수 있도록 값 표시 강화

### 4.5 팀장석 구성 유형 개편

- 전체 도면 기준은 도넛 차트로 제공
- 연도별 추세는 별도 그래프로 제공
- 값 레이블이 막대 밖으로 밀리는 문제를 줄이도록 표현 방식 조정

### 4.6 등간격 지표 개편

- 그래프 중심 구조를 `1순위 / 2순위 / 3순위` 카드형 구조로 변경
- 일자형, L자형을 분리하여 각각 상위 구간과 비중을 표시
- 가장 많이 선택된 구간뿐 아니라 2순위, 3순위도 함께 제시

등간격 구간 기준 변경:

- `1600이하`
- `1601-1750`
- `1751-1900`
- `1901-2050`
- `2051-2200`
- `2201이상`

최종 산출 기준:

- 도면 단위 대표 등간격 값(`등간격_일자형`, `등간격_L자형`)을 수집
- 각 값을 위 구간에 binning
- 구간별 도면 수 비중으로 순위를 산출

### 4.7 그룹 업무 공간 보유 비중 개편

- 전체 도면 기준 값은 큰 수치로 노출
- 연도별 값은 별도 그래프로 제공
- 그래프 없이 값만 보이도록 했던 임시 구조를 개선하여, 전체와 연도별 정보를 명확히 구분

### 4.8 삭제/정리된 구성

- `주요 데스크 채택 현황` 카드 삭제
- 중복되거나 정보 전달력이 낮은 카드 정리

## 5. 숫자 표기 원칙

최종 기준:

- KPI 숫자: 소수점 둘째 자리까지 표시
- 차트 내부 값: 소수점 둘째 자리까지 표시
- 비율 값: `%` 단위 포함 소수점 둘째 자리까지 표시
- 자연수 기반 히트맵/건수 표: 정수로 표시

## 6. 재발 방지 가이드

이번 정비를 통해 반복 확인된 주요 오류 패턴은 다음과 같다.

### 6.1 합산 후 비율 계산 오류

- 연도 전체 면적 합산 후 비율을 내면 큰 도면에 과도한 가중치가 실린다.
- 비율 추이형 지표는 반드시 `도면별 비율 → 평균` 순서인지 검증해야 한다.

### 6.2 평균 분모 오판

- `해당 subtype이 있는 도면만 평균`인지
- `해당 category가 있는 도면 전체를 평균`인지
- `전체 도면 평균`인지

위 세 가지를 PRD와 대시보드에서 동일하게 써야 한다.

### 6.3 표시용 라벨과 원천 데이터값 혼동

- 화면상 `소셜`로 보이더라도 데이터상 프로그램 값은 `공용`일 수 있다.
- 지표 계산은 반드시 원천 데이터 기준으로 수행하고, 화면 라벨은 별도로 관리해야 한다.

### 6.4 화면 표현 변경 시 계산식 영향 검토 필요

- 지표를 카드, 차트, 표 중 어떤 형태로 바꾸더라도 계산식 자체가 바뀌는 것은 아니다.
- UI 개편 전후에 산출 기준이 유지되는지 반드시 확인해야 한다.

## 7. 2026-04-08 작업 이력

### 7.1 업무공간 : 임원 탭 구조 개편

- 상단 주요 지표를 3개로 재구성
- `임원석 최다 구성(실형)`
- `임원실 평균 면적(실형)`
- `오픈 임원석 보유 도면 비율`

### 7.2 오픈 임원석 보유 도면 비율 기준 재확인

검토 기준:

- 사용자가 확인하고자 한 값은 `오픈 임원석 네모가 있는 도면의 개수 비율`

최종 반영 기준:

- `프로그램='임원'`
- `오픈실='오픈'`
- 세부유형은 다음 네 가지 오픈형 임원석 subtype만 포함
- `책상오픈`
- `책상+소파오픈`
- `책상+회의테이블오픈`
- `책상+소파+회의테이블오픈`

분모:

- 현재 필터링된 전체 도면 수

분자:

- 위 조건을 만족하는 네모를 1개 이상 가진 도면 수

검산 메모:

- 전체 540도면 기준 오픈 임원석 보유 도면 수는 110개
- 전체 비율은 20.37%

### 7.3 오픈 임원석 보유 비율 추이 개편

- 연도별 라인 차트에 값 레이블을 직접 표기
- 오픈 임원석 보유 도면 수 / 해당 연도 전체 도면 수 기준으로 재정리
- 계산식 설명이 화면 문구와 일치하도록 수정

### 7.4 임원 가구 구성 비율 개편

- 기존 단일 막대 그래프 구조를 폐지
- `실형 구성 비율`, `오픈형 구성 비율`을 별도 도넛 차트 2개로 분리
- `전체` / `연도별` 토글 추가
- `연도별` 화면에서는 실형, 오픈형 각각을 별도 꺾은선 그래프로 제공

### 7.5 임원 가구 구성별 평균 면적 개편

- 기존 막대 그래프 구조를 폐지
- 실형, 오픈형을 분리한 값 중심 리스트 구조로 변경
- 세부유형별 평균 면적을 그래프가 아니라 직접 읽을 수 있는 값으로 제공

### 7.6 PRD 반영 필요 항목

- 업무공간 : 임원 탭 상단 KPI 정의 변경
- 오픈 임원석 보유 도면 비율의 분자/분모 기준 명시
- 임원 가구 구성 비율의 `전체/연도별` 토글 구조 명시
- 임원 가구 구성별 평균 면적의 값 중심 표현 방식 명시

### 7.7 오픈 임원석 보유 도면 비율 분모 수정

초기 반영 기준:

- `오픈 임원석 보유 도면 수 / 전체 도면 수`

최종 확정 기준:

- `임원공간을 보유한 도면 중, 오픈 임원석 네모를 1개 이상 보유한 도면의 개수 비율`

즉:

- 분자: 오픈 임원석 네모를 1개 이상 가진 도면 수
- 분모: 임원공간을 1개 이상 가진 도면 수

연도별 추이도 같은 기준을 사용한다.

### 7.8 임원 탭 UI 추가 조정

- 상단 중요 KPI 강조 색상을 `임원석 최다 구성(실형)`, `임원실 평균 면적(실형)`에 적용
- `오픈 임원석 보유 비율 추이` 카드 안에 전체 비율 요약값 추가
- 임원 가구 구성 비율은 토글 구조를 제거하고, `전체-실형`, `전체-오픈형`, `연도별-실형`, `연도별-오픈형`의 4개 하위 카드 구조로 변경
- 임원 가구 구성별 평균 면적 라벨은 `책상 + 소파`처럼 공백 포함 형태로 표기
- 연도별 추이형 차트 전반의 좌측 여백을 보정하여 2020년 표기와 세로축 겹침을 완화

## 8. 2026-04-08 회의공간 탭 조정

### 8.1 연도별 추이 차트 표시 방식 보완

- 연도별 회의실 평균 개소, 오픈회의 비중 등 line chart 계열에서 값이 직접 보이도록 데이터 라벨 표시 강화
- line chart 공통 설정에 좌측 패딩과 x축 offset을 추가하여 2020년 값이 세로축과 겹치는 현상을 완화

### 8.2 인원/면적 구간별 회의실 평균 개소 차트 변경

- `인원 구간별 회의실 평균 개소`를 막대그래프에서 꺾은선 그래프로 변경
- `면적 구간별 회의실 평균 개소`를 막대그래프에서 꺾은선 그래프로 변경

### 8.3 회의실 규모별 평균 개소 및 평균 면적 구조 변경

- 기존 차트 2개를 제거
- `4인이하실`, `5-8인실`, `9-16인실`, `17인이상실` 각각에 대해 평균 개소와 평균 면적을 함께 보여주는 KPI형 카드 구조로 변경
- 보조 표 형태의 요약도 함께 제공

### 8.4 회의실 가구 스타일 비중 확장

- 기존 전체 도넛 차트는 유지
- `회의실 규모별 가구 스타일 비중` 차트로 정리하여 4인 이하, 5-8인실, 9-16인실, 17인 이상실별 정형/비정형/Flexible 비중을 함께 제공

### 8.5 전체 오픈 회의 비중 요약 추가

- `연도별 오픈 회의 비중` 카드 안에 `전체 오픈 회의 비중` 요약값을 함께 표시

### 8.6 중복 카드 제거

- `회의실 규모별 핵심값 요약` 카드는 상단 `회의실 규모별 평균 개소 및 평균 면적` 카드와 내용이 중복되어 삭제

## 10. 2026-04-08 단위 전환 성능 개선

- `평 ↔ m²` 단위 전환 시 전체 대시보드를 refresh하지 않고, 해당 차트만 부분 업데이트하도록 구조 개선
- 적용 대상:
  - 업무공간 : 일반 `인당 면적`
  - 소셜공간 `인원 구간별 탕비 평균 면적`
  - 소셜공간 `가용 면적 구간별 탕비 평균 면적`

## 9. 2026-04-08 추세 그래프 공통 표시 원칙 조정

- line chart 계열의 추세 그래프는 세로축 0 시작을 유지하되, 상단 padding과 라벨 위치를 조정하는 방식으로 가시성을 개선
- 값 자체가 헤더에 가려지지 않도록 chart top padding을 확대
- 데이터 라벨이 카드 상단에 붙지 않도록 라벨 offset과 clamp 설정을 조정
## 11. 2026-04-08 소셜공간 산식 및 시각화 개편

- `라운지 계획 비율` 계산 기준을 `라운지_조성여부` 필드에서 `세부유형='라운지' 네모 보유 도면 비율`로 변경
- `2022_퍼시스_송파 가락빌딩_7F_7F` 도면이 라운지 보유 도면으로 반영되도록 기준 정정
- `연도별 소셜공간 유형 구성 비율`에 그래프 내부 퍼센트 라벨 표시 추가
- `인원 구간별 / 가용 면적 구간별 탕비 평균 면적`을 가로 바 차트로 변경
- `라운지 유형별 평균 면적`을 KPI형 값 카드로 변경하고 `평 ↔ m²` 전환 추가
- `라운지 내 세부 프로그램 선택 비율`을 버블 차트로 변경하고 값 라벨을 함께 표시
## 12. 2026-04-09 소셜공간 탭 복구 및 기준 반영

- 소셜공간 탭 수정 과정에서 남아 있던 깨진 helper 함수 때문에 HTML 스크립트 전체가 파싱되지 않던 문제를 복구
- `라운지 계획 비율`을 `라운지_조성여부` 필드 기준이 아니라 `세부유형='라운지' 네모를 1개 이상 보유한 도면 비율` 기준으로 정정
- `2022_퍼시스_송파 가락빌딩_7F_7F` 도면을 라운지 보유 도면에 포함되도록 반영
- `연도별 소셜공간 유형 구성 비율`에 막대 내부 값 표기 추가
- `인원 구간별 / 가용 면적 구간별 탕비 평균 면적`을 가로 바 차트로 변경
- `라운지 유형별 평균 면적`을 KPI형 값 카드로 변경하고 `평 ↔ m²` 전환 기능 추가
- `라운지 내 세부 프로그램 선택 비율`을 버블 차트로 변경하고 버블 내부 값 라벨을 함께 표시
## 13. 2026-04-09 소셜공간 카드 표현 조정

- `연도별 라운지 계획 여부` 카드에 `전체 라운지 계획 비율` 요약값을 별도 표시해 연도별 추이와 구분되도록 정리
- `인원 구간별 / 가용 면적 구간별 탕비 평균 면적`을 가로 바 차트에서 꺾은선 그래프로 변경
- `라운지 내 세부 프로그램 선택 비율` 버블 차트를 packed-bubble 스타일에 가깝게 재배치하고 버블 내부에 프로그램명과 비율이 함께 보이도록 조정
